# Compose

Local integration topology will live here. The first productionization milestone will add MySQL, Redis, the control plane, solver workers, and the web console with health checks and persistent volumes.
